<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SaveUp.View.MainPage"
             xmlns:viewModel="clr-namespace:SaveUp.ViewModel">
    <ContentPage.BindingContext>
        <viewModel:MainPageVM/>
    </ContentPage.BindingContext>

    <StackLayout>
        <Frame BackgroundColor="Green" Padding="24" CornerRadius="0">
            <Label Text="Save Up!" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>
        <Label Text='Sie können hier eingeben, wie viel Geld Sie gespart haben.' FontSize="16" Padding="30,0,30,0"/>
        <Entry Text="{Binding Geld}" Keyboard="Numeric"/>
        <Entry Placeholder="Detail" Text="{Binding Detail}"/>
        <Button Text="Einfügen" Command="{Binding Einfügen}" Margin="10" BackgroundColor="Transparent" BorderColor="Silver" BorderWidth="1" BorderRadius="2" TextColor="Gray"/>
        <Button Text="Einträge" Command="{Binding Einträge}" Margin="10" BackgroundColor="Transparent" BorderColor="Silver" BorderWidth="1" BorderRadius="2" TextColor="Gray"/>
        <Button Text="Einträge" Command="{Binding Delete}" Margin="10" BackgroundColor="Transparent" BorderColor="Silver" BorderWidth="1" BorderRadius="2" TextColor="Gray"/>
    </StackLayout>

</ContentPage>
